

<% if current_user %>
  <%= render "dashboard" %>
<% else %>
  <%= render "intro" %>
<% end %>


        <div class="ui purple inverted menu">
          <i class="home icon"></i>
            <% if current_user %>
              <%= link_to "Mailbox", conversations_path, class: "item" %>
            <% end %>
        </div>
      </div>
